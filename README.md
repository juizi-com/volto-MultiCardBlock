# volto-MultiCardBlock
A block to define a number of cards, where each card defines a background image, text and button. Editors can define number of columns available on desktop (max 5) and text alignment.
